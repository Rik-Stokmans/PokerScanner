name: poker_scanner
description: "The Silent Table - A premium poker scanning and analysis app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  go_router: ^13.0.0
  fl_chart: ^0.68.0
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.2.0
  flutter_riverpod: ^2.5.1
  shared_preferences: ^2.3.1
  flutter_blue_plus: ^1.35.3
  permission_handler: ^11.3.1
  share_plus: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
